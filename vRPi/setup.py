from distutils.core import setup, Extension

setup(name             = 'RPi.GPIO',
      version          = '0.6.2',
      license          = 'MIT',
      packages         = ['RPi'],
      py_modules       = ['RPi.GPIO'])
